<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="testMapper">
    <select id="getList">
        SELECT * FROM todo WHERE id = #{id}
    </select>

    <insert id="insert">
        insert into todo (num, id, contents, regdate) values (nextval('todo_seq'), #{id}, #{contents}, now())
    </insert>

    <delete id="delete">
        delete from todo where num = #{num}
    </delete>

    <update id="update">
        update todo set contents = #{contents} where num = #{num}
    </update>

    <update id="done">
        update todo set done = true, donedate = now() where num = #{num}
    </update>
</mapper>